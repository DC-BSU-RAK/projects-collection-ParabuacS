<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CharacterCodex">

        <!--This move was suggested by Gemini, the app will start off using SplashActivity
        instead of MainActivity to access the splash screen-->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--activity_main-->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
        <!--activity_creator-->
        <activity
            android:name=".activities.CreatorActivity"
            android:exported="false" />
        <!--activity_profile-->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />
        <!--activity_details-->
        <activity
            android:name=".activities.DetailsActivity"
            android:exported="false" />
    </application>
</manifest>